# Pilot một camera: gửi toàn ảnh để Gemini đọc QR + số cân; QR local kiểm tra độc lập.
# Sao chép thành %LOCALAPPDATA%\TramCanQR\config.env khi dùng bản Windows.

ROLL_SCALE_STATION_COUNT=1
ROLL_SCALE_GATEWAY_ID=gateway-01
ROLL_SCALE_STATION_IDS=station-01
ROLL_SCALE_CAMERA_IDS=camera-01
ROLL_SCALE_PORT=8080
ROLL_SCALE_WEIGHT_BURST_FRAMES=5

ROLL_SCALE_WEIGHT_ENGINE=gemini
ROLL_SCALE_GEMINI_API_KEY=replace-with-google-ai-studio-key
ROLL_SCALE_GEMINI_MODEL=gemini-3.5-flash-lite
ROLL_SCALE_GEMINI_ACCURATE_MODEL=gemini-3.1-pro-preview
ROLL_SCALE_GEMINI_TIMEOUT=10.0
ROLL_SCALE_GEMINI_ACCURATE_TIMEOUT=30.0

# Chế độ Gemini toàn ảnh không dùng ROI. Giữ camera đủ gần để LED và QR rõ nét.

# Cloudinary/Supabase không bắt buộc cho pilot đọc QR + cân.
